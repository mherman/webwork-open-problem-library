##DESCRIPTION
##ENDDESCRIPTION
## DBsubject(Calculus - single variable)
## DBchapter(Infinite sequences and series)
## DBsection(Limit of a sequence)
## Institution(Univeristy of Utah)
## Author(Utah ww group)
## MLT(Terms2Limit)
## Level(2)
## TitleText1('Calculus')
## AuthorText1('Dale Varberg, Edwin J. Purcell, and Steve E. Rigdon')
## EditionText1('9')
## Section1('.')
## Problem1('')
## KEYWORDS('calculus')

DOCUMENT(); # This should be the first executable line in the problem.

loadMacros(
  "PGstandard.pl",
  "MathObjects.pl",
);

TEXT(beginproblem());
######################################
#  Setup
Context("Numeric");
Context()->variables->add(a=> 'Real', D=> 'Real', A=> 'Real', B=> 'Real', C=> 'Real', n=> 'Real');

$a = Compute("a");
$A = Compute("A");
$B = Compute("B");
$C = Compute("C");
$D = Compute("D");
$n = Compute("n");

$func = Compute(" ( ln(($a)/($A*$n + $B) ) ) /(sqrt($C * $n + $D )) ")->reduce;
Context()->texStrings;
######################################

#  Main text


BEGIN_TEXT
Consider the sequence given by \( \displaystyle x_n = $func \), with \(a,A,C \gt 0\). Compute the first five terms of the sequence, \(x_1\), ... , \(x_5\). Now compute \( \displaystyle \lim_{n \to \infty} x_n\). Your answers should be in tems of \(a\), \(A\), \(B\), \(C\), and \(D\).
$PAR
First five terms: \{ans_rule(30)\}
$BR
$BBOLD Answer with the terms in order, separated by commas. That is, answer with an ordered list. $EBOLD
$PAR
\( \displaystyle \lim_{n \to \infty} x_n = \)\{ans_rule(30)\}
END_TEXT


######################################
Context()->normalStrings;
#  Answer

$n = 0;
@ans = (1,1,1,1,1,1);
for my $n (1 .. 5) 
{
$ans[$n ] = Compute(" ( ln(($a)/($A*$n + $B) ) ) /(sqrt($C * $n + $D )) ")->reduce;
}

shift(@ans);

$anslist = List(@ans);
ANS($anslist->cmp,
         Compute("0")->cmp);




#ANS($list->cmp(ordered=> 1));

$showPartialCorrectAnswers = 1;

######################################


COMMENT('MathObject version');
ENDDOCUMENT();
